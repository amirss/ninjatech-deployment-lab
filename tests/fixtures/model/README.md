# Recorded model fixtures

These synthetic files exercise the offline `RecordedModelProvider`. A fixture-set name is
trusted configuration, never task input. Every exchange binds one canonical request
fingerprint to one strict response action.

Milestone 5A1 supplies the loader and contract fixtures only. Milestone 5A2 will add
workflow-specific multi-step recordings after repository context orchestration exists.
